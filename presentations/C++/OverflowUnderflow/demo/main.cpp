#include <iostream>

using namespace std;

int main ()
{
	cout << (short)(-32'769)  << " " << (unsigned int)(-1) << "\n";
	cout << (unsigned long long)(-1) << "\n";
	cout << -((long long)((unsigned long long)(-1) / 2)) << "\n";
}
