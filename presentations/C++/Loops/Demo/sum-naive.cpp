#include <iostream>
using namespace std;

int main()
{
    int n, num, sum = 0;
    cin >> n;

    if (n > 0)
    {
        cin >> num;
        sum += num;
    }
    if (n > 1)
    {
        cin >> num;
        sum += num;
    }
    if (n > 2)
    {
        cin >> num;
        sum += num;
    }
    if (n > 3)
    {
        cin >> num;
        sum += num;
    }
    if (n > 4)
    {
        cin >> num;
        sum += num;
    }
    if (n > 5)
    {
        cin >> num;
        sum += num;
    }
    if (n > 6)
    {
        cin >> num;
        sum += num;
    }
    if (n > 7)
    {
        cin >> num;
        sum += num;
    }
    if (n > 8)
    {
        cin >> num;
        sum += num;
    }
    if (n > 9)
    {
        cin >> num;
        sum += num;
    }
    if (n > 10)
    {
        cin >> num;
        sum += num;
    }
    if (n > 11)
    {
        cin >> num;
        sum += num;
    }
    if (n > 12)
    {
        cin >> num;
        sum += num;
    }
    if (n > 13)
    {
        cin >> num;
        sum += num;
    }
    if (n > 14)
    {
        cin >> num;
        sum += num;
    }
    if (n > 15)
    {
        cin >> num;
        sum += num;
    }
    if (n > 16)
    {
        cin >> num;
        sum += num;
    }
    if (n > 17)
    {
        cin >> num;
        sum += num;
    }
    if (n > 18)
    {
        cin >> num;
        sum += num;
    }
    if (n > 19)
    {
        cin >> num;
        sum += num;
    }

    cout << sum << endl;
    return 0;
}
